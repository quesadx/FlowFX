package cr.ac.una.flowfx.controller;

import javafx.fxml.Initializable;
import java.net.URL;
import java.util.ResourceBundle;

/**
 * Placeholder controller not used; kept to avoid broken references if any FXML mentions it.
 */
public class PersonSelectorBridgeController extends Controller implements Initializable {
    @Override
    public void initialize(URL url, ResourceBundle rb) { }
    @Override
    public void initialize() { }
}
